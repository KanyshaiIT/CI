name: Hello Workflow
on:
  push:
    branches:
      - main
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - Print greeting
        run: echo "Hello Devops students for sunday class!!!"
      - name: Step 2 - show current directory
        run: pwd
      - name: Step 3 - List files
        run: ls - la
